import React from "react";
import { LayoutContainer } from "components/shared/Layout";

export const DashboardContainer: React.VFC = () => {
  return (
    <LayoutContainer title="ダッシュボード">
      <div>implement a dashboard feature here.</div>
    </LayoutContainer>
  );
};
